@model IEnumerable<FacturationApp.Models.Facture>

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Liste des factures</h2>
        <a href="/Facture/Create" class="btn btn-primary">Ajouter une facture</a>
    </div>

    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Numéro</th>
                <th>Date</th>
                <th>Client</th>
                <th>Total HT</th>
                <th>Total TTC</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var f in Model)
            {
                <tr>
                    <td>@f.Numero</td>
                    <td>@f.Date.ToString("yyyy-MM-dd HH:mm")</td>
                    <td>@f.Client.Nom</td>
                    <td>@f.TotalHT.ToString("C")</td>
                    <td>@f.TotalTTC.ToString("C")</td>
                    <td>
                        <a href="/Facture/GenerateFacture/@f.Id" class="btn btn-info btn-sm">Voir</a>
                        <a href="/Facture/Edit/@f.Id" class="btn btn-warning btn-sm">Modifier</a>
                        <a href="/Facture/Delete/@f.Id" class="btn btn-danger btn-sm">Supprimer</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
